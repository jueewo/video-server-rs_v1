---
import type { CollectionEntry } from "astro:content";

interface Props {
    card: CollectionEntry;
}
const { card } = Astro.props;
---

{/* CARDDRAFTOVERLAY */}
{/* Clickable draft content - shows badge and key icon */}
{
    card.data.draft_content && card.data.draft_content_clickable && (
        <div class="flex items-center mt-1 ml-2">
            <span class="inline-flex items-center bg-amber-500 text-amber-950 text-xs font-medium px-2 py-0.5 rounded-full">
                {card.data.draft_content_msg &&
                card.data.draft_content_msg.length > 0 ? (
                    <span>{card.data.draft_content_msg}</span>
                ) : (
                    "under review"
                )}
            </span>

            <div class="ml-4 w-6 h-6">
                {/* key icon @ top */}
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke-width="1.5"
                    stroke="currentColor"
                    class="w-full h-full stroke-amber-500"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        d="M15.75 5.25a3 3 0 013 3m3 0a6 6 0 01-7.029 5.912c-.563-.097-1.159.026-1.563.43L10.5 17.25H8.25v2.25H6v2.25H2.25v-2.818c0-.597.237-1.17.659-1.591l6.499-6.499c.404-.404.527-1 .43-1.563A6 6 0 1121.75 8.25z"
                    />
                </svg>
            </div>
        </div>
    )
}

{/*  Non-clickable draft content - shows clock overlay */}
{
    card.data.draft_content && !card.data.draft_content_clickable && (
        <div class="absolute inset-0 bg-black/80 rounded-xl">
            {/* overlay locked clock icon */}
            <div class="w-full h-full flex justify-center items-center">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke-width="1.5"
                    stroke="currentColor"
                    class="w-16 h-16 stroke-amber-500"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        d="M12 6v6h4.5m4.5 0a9 9 0 11-18 0 9 9 0 0118 0z"
                    />
                </svg>
            </div>
        </div>

        <div class="absolute inset-0">
            {/* overlay locked draft_content_msg */}
            <div class="flex items-center mt-1 ml-2">
                <span class="inline-flex items-center bg-amber-500 text-amber-950 text-xs font-medium px-2 py-0.5 rounded-full">
                    {card.data.draft_content_msg &&
                    card.data.draft_content_msg.length > 0 ? (
                        <span>{card.data.draft_content_msg}</span>
                    ) : (
                        "under review"
                    )}
                </span>
            </div>
        </div>
    )
}
